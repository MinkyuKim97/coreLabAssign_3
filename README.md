# Tree-node shape project manager
Currently, it's based on the sample JSON.
You can download JSON file after customizing your own project details
After clone the repo, shift the 'projectData.json' into your own JSON
